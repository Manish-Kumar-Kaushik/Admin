import React from "react";
import ClusterDetails from "@/components/dashboard/review-intelligence/cluster-details";

export const metadata = {
  title: "Review Cluster Details - Review Intelligence",
};

export default function ClusterDetailsPage() {
  return <ClusterDetails />;
}
