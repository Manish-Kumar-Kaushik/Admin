import NotificationCenter from "@/components/dashboard/settings/notification-center";

export const metadata = {
  title: "Notification Center | Settings | BuyWish",
  description: "Monitor system activity, alerts, and important updates across the platform.",
};

export default function NotificationCenterPage() {
  return <NotificationCenter />;
}
